# Docker Compose - Production Configuration
# This is a reference to the actual file in deployment/docker/
# Use: docker-compose -f deployment/docker/docker-compose.prod.yml up

# For production, use:
# docker-compose -f deployment/docker/docker-compose.prod.yml up -d

# For local development, use:
# docker-compose -f deployment/docker/docker-compose.dev.yml up -d

# Or use the Makefile:
# make docker-up-dev    (starts development stack)
# make docker-up-prod   (starts production stack)
# make docker-down      (stops all containers)

version: "3.9"

services:
  # Redirect to production compose file
  # See: deployment/docker/docker-compose.prod.yml
  note:
    image: alpine:latest
    command: echo "Please use deployment/docker/docker-compose.prod.yml or docker-compose.dev.yml"
